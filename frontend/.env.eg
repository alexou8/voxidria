# =============================================================================
# Voxidria Frontend — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
#
# VITE_ prefix exposes variables to the browser bundle.
# Only put PUBLIC (non-secret) values here.
# =============================================================================

# Auth0 — SPA configuration
# Found in: Auth0 Dashboard > Applications > Your App > Settings
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=

# The API identifier (audience) — must match AUTH0_AUDIENCE in backend/.env
VITE_AUTH0_AUDIENCE=

# Supabase — PUBLIC keys only (safe to expose in the browser)
# Found in: Supabase > Project Settings > API > Project URL and anon/public key
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App URL (used for Auth0 redirect URIs in production)
VITE_APP_URL=

# Gemini browser-side settings (prototype/local persistence chat)
# NOTE: This key is exposed to the browser. For production security, proxy through backend.
VITE_GEMINI_API_KEY=

# Requested Gemini model (if unavailable for your key, app falls back automatically)
# Recommended stable option: gemini-2.0-flash
VITE_GEMINI_MODEL=gemini-2.0-flash

# Optional: Vox tone controls (used in Gemini prompt instructions)
# Example: "friendly, informative, medically professional, concise"
VITE_VOX_PERSONALITY=friendly, informative, medically professional

# Optional OpenAI fallback (browser-side, exposed to client bundle)
# Used only if Gemini requests fail or Gemini key is missing.
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini
