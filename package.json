{
  "name": "voxidria",
  "version": "1.0.0",
  "private": true,
  "description": "Voice-based Parkinson's risk estimation platform",
  "scripts": {
    "dev": "cd client && npm run dev",
    "install:all": "cd client && npm install",
    "build": "cd client && npm run build"
  }
}
