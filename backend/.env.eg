# =============================================================================
# Voxidria Backend — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit the .env file to version control.
#
# Where to get each value is documented inline.
# =============================================================================

# -----------------------------------------------------------------------------
# Auth0 — https://manage.auth0.com/
# Create an API under Applications > APIs.
# Your tenant domain is in Settings > General > Domain.
# -----------------------------------------------------------------------------

# Your Auth0 tenant domain
AUTH0_DOMAIN=

# The API identifier (audience) set when creating the Auth0 API
AUTH0_AUDIENCE=

# Full issuer URL — always the tenant domain with a trailing slash
AUTH0_ISSUER_BASE_URL=

# Shared secret for the Auth0 Actions webhook (only needed if using webhook-auth0 function)
# Generate with: openssl rand -hex 32
AUTH0_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Supabase — https://app.supabase.com/
# Go to: Project Settings > API
# IMPORTANT: SUPABASE_SERVICE_ROLE_KEY bypasses Row Level Security.
#   It must ONLY be used in server-side environments (Edge Functions).
#   Never expose it to the frontend or include it in client-side code.
# -----------------------------------------------------------------------------

# Your Supabase project URL (found in Project Settings > API > Project URL)
SUPABASE_URL=

# Service Role key — server-side ONLY. Never send this to the browser.
# Found in: Project Settings > API > Project API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=

# Anon/public key — safe to expose in the frontend (used by client SDK)
# Found in: Project Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# Google Gemini — https://aistudio.google.com/app/apikey
# GEMINI_API_KEY is server-side only. Never expose to the browser.
# -----------------------------------------------------------------------------

# Gemini API key
GEMINI_API_KEY=

# Model to use for reading analysis (gemini-1.5-pro recommended for accuracy)
GEMINI_MODEL=

# -----------------------------------------------------------------------------
# ElevenLabs — https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_API_KEY is server-side only. Never expose to the browser.
# -----------------------------------------------------------------------------

# ElevenLabs API key
ELEVENLABS_API_KEY=

# Voice ID to use for TTS — find in ElevenLabs > Voices (e.g., Rachel = 21m00Tcm4TlvDq8ikWAM)
ELEVENLABS_VOICE_ID=

# Optional: dedicated voice ID for MEDICAL_ASSISTANT mode.
# If omitted, ELEVENLABS_VOICE_ID is used.
ELEVENLABS_MEDICAL_ASSISTANT_VOICE_ID=

# Model ID (eleven_multilingual_v2 supports multiple languages)
ELEVENLABS_MODEL_ID=

# -----------------------------------------------------------------------------
# CORS — Restrict Edge Function access to your frontend domain
# -----------------------------------------------------------------------------

# Your frontend's origin (no trailing slash)
# Development: http://localhost:5173
# Production: https://your-app.vercel.app
ALLOWED_ORIGIN=http://localhost:5173

# -----------------------------------------------------------------------------
# Optional: Inference Microservice
# Only needed if you run a separate Python/FastAPI ML inference service.
# The microservice handles SUSTAINED_VOWEL and DDK task analysis.
# -----------------------------------------------------------------------------

# Base URL of the inference service (e.g., https://inference.your-domain.com)
INFERENCE_SERVICE_URL=

# Shared secret for authenticating Edge Function → inference service calls.
# Generate with: openssl rand -hex 32
INFERENCE_SHARED_SECRET=
